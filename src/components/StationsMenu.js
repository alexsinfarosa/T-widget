import React from "react";
import { stations } from "stations";

const StationsMenu = ({ store }) => {
  console.log(store.isLoading);
  console.log(stations);
  return <div />;
};

export default StationsMenu;
